[package]
name = "momentum-pumpswap"
version = "0.1.0"
edition.workspace = true

[dependencies]
momentum-core = { version = "0.1.0", path = "../core" }
solana-pubkey = "3"

[dev-dependencies]
base64 = "0.23.1"
