[package]
name = "momentum-ingest"
version = "0.1.0"
edition.workspace = true

[dependencies]
momentum-core = { version = "0.1.0", path = "../core" }
momentum-pump = { version = "0.1.0", path = "../pump" }
momentum-token2022 = { version = "0.1.0", path = "../token2022" }
solana-pubkey = "3"

[dev-dependencies]
base64 = "0.23.1"
